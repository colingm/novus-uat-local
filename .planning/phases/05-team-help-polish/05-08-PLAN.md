---
phase: 05-team-help-polish
plan: 08
type: execute
wave: 6
depends_on: ["05-04"]
files_modified:
  - src/team/components/InviteTeammateModal.tsx
  - .planning/phases/05-team-help-polish/05-04-SUMMARY.md
autonomous: true
requirements: [TEAM-02, UI-02]
gap_closure: true

must_haves:
  truths:
    - "Opening the Invite teammate modal -> filling email + role -> submitting -> reopening the modal renders an empty email field and the default 'Member' role (Gap 2 closed: Test 1 sub-issue 'kept the values from the previous invite')"
    - "Opening the Invite teammate modal -> filling email + role -> cancelling (Cancel button or backdrop / Escape close) -> reopening the modal renders an empty email field and the default 'Member' role (form state DOES NOT leak across cancelled opens either)"
    - "Mounting the Invite teammate modal produces ZERO React 19 hydration / nesting console errors — specifically the 'In HTML, <h3> cannot be a child of <h2>' message is gone (Gap 3 closed: Test 4 console-error issue)"
    - "Modal title still renders with Mantine's default ModalTitle styling (string title prop matches TaskFormModal:273 / ResetDemoDataModal:128 / DeleteConfirmModal:48 precedent — DOM is `<h2 class=ModalTitle>Invite teammate</h2>` with no nested heading)"
    - "Implementation mirrors Phase 4 CR-01 reset pattern verbatim: defaultValues hoisted into a useMemo-stabilized object; prevOpenedRef + useEffect calls form.reset(defaultValues) on the false->true `opened` transition (no `mode === 'create'` guard — InviteTeammateModal has no edit mode)"
    - "The misleading line-16 doc comment claiming `keepMounted={false} drops RHF state on close` is corrected to describe the actual reset mechanism (the useEffect-driven form.reset on open transition; keepMounted is acknowledged as decorative since the InviteTeammateModal outer component holding the useForm hook stays mounted unconditionally by TeamPage)"
    - "Unused @mantine/core `Title` import is removed from InviteTeammateModal.tsx; remaining import list still includes Modal, Stack, Group (the elements still in use)"
    - "05-04-SUMMARY.md patterns-established line 52 ('Modal title as <Title order={3}> JSX element (not string prop) per Phase 4 modal-nesting fix') is corrected to reflect actual Phase 4 precedent (TaskFormModal:273 passes a plain string; Mantine's ModalBaseTitle wraps it in <h2>)"
    - "Modal still carries `data-pendo-id={PENDO_IDS.team.invite.modalContainer}` on the outer Mantine <Modal> for guide targeting (UI-02 / D-14 unchanged)"
    - "InviteFormSchema with .superRefine email-dedup (D-03) is preserved verbatim; createTeammate / findTeammateByEmail wiring unchanged; toast copy 'Invite sent — Sent to {email}' unchanged"
    - "npm run typecheck, npm run lint, npm run build all exit 0; no new ESLint warnings introduced"
  artifacts:
    - path: "src/team/components/InviteTeammateModal.tsx"
      provides: "RHF + Zod invite modal with: (1) defaultValues useMemo-stabilized, (2) prevOpenedRef + open-transition form.reset effect, (3) string `title` prop on Mantine Modal (no nested Title), (4) corrected line-16 doc comment, (5) no unused Title import"
      contains: "prevOpenedRef"
    - path: ".planning/phases/05-team-help-polish/05-04-SUMMARY.md"
      provides: "Corrected patterns-established entry — modal titles use plain string per Phase 4 precedent"
  key_links:
    - from: "src/team/components/InviteTeammateModal.tsx (open-transition useEffect)"
      to: "src/tasks/components/TaskFormModal.tsx lines 149-165 (CR-01 reference precedent)"
      via: "prevOpenedRef + form.reset(defaultValues) pattern"
      pattern: "prevOpenedRef"
    - from: "src/team/components/InviteTeammateModal.tsx Modal title prop"
      to: "Mantine ModalBaseTitle (renders <h2>)"
      via: "Plain string title (no Title wrapper)"
      pattern: "title=\"Invite teammate\""
---

<objective>
Resolve Gap 2 (Invite teammate modal form-state leak between opens) and Gap 3 (h3-in-h2 hydration error) from `.planning/phases/05-team-help-polish/05-UAT.md`. Both bugs live in the same file (`src/team/components/InviteTeammateModal.tsx`), have surgical fixes specified verbatim in the diagnostic sessions, and are bundled into one plan because: (a) atomic-commit hygiene benefits from a single "InviteTeammateModal cleanup" commit rather than two near-trivial commits, (b) splitting would create a serial dependency for ~30 lines of edits in one file, (c) both edits change the same component file so parallel execution offers no benefit over sequential reasoning.

Purpose: Per `.planning/debug/05-invite-modal-state-leak.md`, Phase 5 Plan 04 ported the `keepMounted={false}` idiom from TaskFormModal but missed the accompanying `prevOpenedRef` + open-transition `form.reset()` effect (Phase 4 CR-01) that is the actual fix for cross-open form-state retention. The line-16 doc comment in InviteTeammateModal.tsx locked in the misconception. Per `.planning/debug/05-invite-modal-h3-in-h2.md`, the same Plan 04 wrapped the modal title in `<Title order={3}>` against the actual Phase 4 precedent (TaskFormModal:273, ResetDemoDataModal:128, DeleteConfirmModal:48 all pass plain strings) and the 05-04-SUMMARY.md patterns-established line misstates the precedent. This plan ports the CR-01 reset pattern verbatim, swaps the title prop to a plain string, removes the unused Title import, corrects the doc comment, and corrects the SUMMARY's patterns line.

Output: 1 component file modified (~15 lines net change — additions for useMemo + useEffect + useRef + import update; deletions for the Title JSX wrapper and the unused import); 1 SUMMARY file modified (1-line patterns correction). No new files. No PENDO_IDS changes. No teamsRepo changes. No schema or storage changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-team-help-polish/05-CONTEXT.md
@.planning/phases/05-team-help-polish/05-UAT.md
@.planning/debug/05-invite-modal-state-leak.md
@.planning/debug/05-invite-modal-h3-in-h2.md
@.planning/phases/05-team-help-polish/05-04-SUMMARY.md
@src/team/components/InviteTeammateModal.tsx
@src/tasks/components/TaskFormModal.tsx
@src/settings/ResetDemoDataModal.tsx
@src/tasks/components/DeleteConfirmModal.tsx

<interfaces>
Phase 4 CR-01 reference precedent (src/tasks/components/TaskFormModal.tsx lines 107-165):
- defaultValues hoisted into a useMemo with explicit primitive-level deps:
  `const defaultValues: TaskFormValues = useMemo(() => ({ ... }), [mode, initialTask, visitor.id, visitor.firstName, visitor.lastName])`
- prevOpenedRef tracking the prior `opened` value:
  `const prevOpenedRef = useRef(opened)`
- useEffect calling form.reset(defaultValues) on false->true open transition:
  ```
  useEffect(() => {
    const wasClosed = !prevOpenedRef.current
    prevOpenedRef.current = opened
    if (opened && wasClosed && mode === 'create') {
      form.reset(defaultValues)
    }
  }, [opened, mode, form, defaultValues])
  ```
- For InviteTeammateModal: drop the `mode === 'create'` guard — the component has only one mode.

Mantine title-prop precedent (string, not JSX):
- src/tasks/components/TaskFormModal.tsx:273 — `title={mode === 'create' ? 'New task' : 'Edit task'}`
- src/settings/ResetDemoDataModal.tsx:128 — `title="Reset demo data?"`
- src/tasks/components/DeleteConfirmModal.tsx:48 — `title="Delete this task?"`
- Mantine ModalBaseTitle renders `<Box component="h2">` unconditionally — passing JSX with `<Title order={3}>` produces `<h2><h3>...</h3></h2>` which React 19 flags at hydration.

Current state of src/team/components/InviteTeammateModal.tsx (the file to fix):
- Imports `Modal, Stack, Group, Title` from '@mantine/core' (line 23) — Title becomes unused after this fix.
- Line 16 doc comment: "keepMounted={false} drops RHF state on close (mirrors TaskFormModal D-26 idiom)." — INCORRECT (Mantine's keepMounted only controls internal transition node, not the React component holding useForm).
- Line 67-71: useForm with inline defaultValues object — NOT memoized.
- No useEffect, no useRef, no form.reset() call anywhere — the entire CR-01 reset pattern is missing.
- Line 108: `title={<Title order={3}>Invite teammate</Title>}` — the h3-in-h2 bug.
- Line 111: `keepMounted={false}` — decorative (Mantine default already drops the modal's internal DOM); not load-bearing once the useEffect-driven reset lands.

Component is rendered unconditionally by TeamPage.tsx (lines 91-96), so InviteTeammateModal stays mounted for the entire /app/team lifetime — the useForm hook survives every open/submit/close cycle without the explicit reset.

createTeammate / findTeammateByEmail from src/team/teamsRepo (unchanged) — preserve verbatim.
PENDO_IDS.team.invite.modalContainer / modalEmail / modalRole / modalCancel / modalSubmit (unchanged) — preserve verbatim.
@mantine/notifications + IconCheck toast pattern (unchanged) — preserve verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port CR-01 reset pattern + fix h3-in-h2 + cleanup unused import + doc comment in InviteTeammateModal.tsx</name>
  <files>src/team/components/InviteTeammateModal.tsx</files>
  <read_first>
    - src/team/components/InviteTeammateModal.tsx (entire file — 163 lines; the whole file is the change surface)
    - src/tasks/components/TaskFormModal.tsx lines 37-47 (imports — note `useEffect, useMemo, useRef` from 'react'), lines 107-165 (the CR-01 reset implementation verbatim — the pattern to port; drop the `mode === 'create'` guard for InviteTeammateModal), line 273 (string title prop precedent)
    - src/settings/ResetDemoDataModal.tsx line 128 (string title precedent)
    - src/tasks/components/DeleteConfirmModal.tsx line 48 (string title precedent)
    - .planning/debug/05-invite-modal-state-leak.md (Resolution + Evidence sections — verbatim fix shape)
    - .planning/debug/05-invite-modal-h3-in-h2.md (Resolution + Evidence sections — verbatim fix shape)
    - .planning/phases/05-team-help-polish/05-UAT.md test 1 + test 4 (gap symptoms confirming the user-observable behavior the fix must restore)
  </read_first>
  <action>
    Apply five surgical edits to `src/team/components/InviteTeammateModal.tsx`. All edits are local to this file. NO behavior change to validation, dedupe refine, createTeammate call, toast, or PENDO_IDS wiring — those are correct and stay verbatim.

    EDIT 1 — Imports (line 19 and line 23):
    - Change the React import from `import { useMemo } from 'react'` to `import { useEffect, useMemo, useRef } from 'react'`. Preserve alphabetical order convention (useEffect, useMemo, useRef).
    - Change the @mantine/core import from `import { Modal, Stack, Group, Title } from '@mantine/core'` to `import { Modal, Stack, Group } from '@mantine/core'` (Title removed — it becomes unused after EDIT 3).

    EDIT 2 — Stabilize defaultValues with useMemo (replace the inline object at the existing useForm call, currently around line 70):
    - Before the `useForm` call (between the `type InviteFormValues = z.infer<typeof InviteFormSchema>` line and the `const form = useForm<InviteFormValues>(...)` line), add a useMemo-stabilized defaultValues binding:
      `const defaultValues: InviteFormValues = useMemo(() => ({ email: '', workspaceRole: 'Member' as const }), [])`
      - Empty dependency array: defaults are constants for this modal (no mode flip, no edit-mode initialTask).
      - `'Member' as const` keeps the WorkspaceRole literal type narrow.
    - Change the useForm `defaultValues` from the inline object literal `{ email: '', workspaceRole: 'Member' }` to the new stable `defaultValues` binding.
    - The useForm call now reads: `const form = useForm<InviteFormValues>({ resolver: zodResolver(InviteFormSchema), mode: 'onSubmit', defaultValues })`.

    EDIT 3 — Add prevOpenedRef + open-transition reset useEffect (CR-01 port; drop `mode === 'create'` guard):
    - Immediately after the `const form = useForm<InviteFormValues>(...)` call, add:
      ```
      const prevOpenedRef = useRef(opened)
      useEffect(() => {
        const wasClosed = !prevOpenedRef.current
        prevOpenedRef.current = opened
        if (opened && wasClosed) {
          form.reset(defaultValues)
        }
      }, [opened, form, defaultValues])
      ```
    - The effect deps array is [opened, form, defaultValues] — NO `mode` (this modal has no edit mode).
    - This is verbatim CR-01 from TaskFormModal.tsx:158-165 minus the `&& mode === 'create'` clause.
    - Add a brief comment immediately above the prevOpenedRef declaration referencing CR-01: `// Open-transition reset (Plan 08 — Gap 2 fix). Mirrors TaskFormModal.tsx CR-01:158-165. InviteTeammateModal stays mounted by TeamPage across opens, so useForm's internal state persists — this effect resets defaults on the false->true `opened` transition. No `mode === 'create'` guard: this modal has only one mode.`

    EDIT 4 — Modal title prop swap (line 108):
    - Change `title={<Title order={3}>Invite teammate</Title>}` to `title="Invite teammate"`.
    - Per .planning/debug/05-invite-modal-h3-in-h2.md, Mantine's ModalBaseTitle wraps the title slot in `<Box component="h2">` unconditionally. Passing a plain string produces valid `<h2>Invite teammate</h2>` (matches TaskFormModal:273, ResetDemoDataModal:128, DeleteConfirmModal:48 precedent).
    - The `<Title order={3}>` JSX is GONE. No other usage of Title remains in the file (EDIT 1 already removed the import).
    - `keepMounted={false}` on line 111 STAYS — it is decorative now but documents intent and is harmless. Do NOT delete it (out of scope for this gap closure; if a future refactor wants to remove it, that's a separate plan).

    EDIT 5 — Correct the file-header doc comment (line 16):
    - Replace the misleading line "keepMounted={false} drops RHF state on close (mirrors TaskFormModal D-26 idiom)." with an accurate description of the actual reset mechanism. Suggested replacement (concise, technically correct):
      ```
      Form state reset on reopen is owned by the open-transition useEffect that
      calls form.reset(defaultValues) on the false->true `opened` transition
      (mirrors TaskFormModal.tsx CR-01 fix at lines 149-165). InviteTeammateModal
      stays mounted by TeamPage across opens — useForm's internal state would
      otherwise survive every open/submit/close cycle. The `keepMounted={false}`
      on the inner Mantine <Modal> only controls Mantine's internal transitioned
      DOM, NOT the outer React component holding the useForm hook; it is kept as
      decorative (matches Mantine's default behavior anyway) but the actual
      reset behavior comes from the useEffect.
      ```
    - This doc comment is a multi-line block comment; preserve the surrounding /* ... */ structure of the existing file-header docblock.

    NO OTHER CHANGES. Specifically DO NOT touch:
    - The InviteFormSchema useMemo + zodResolver wiring (preserves D-03 dedupe).
    - The onSubmit handler (firstName derivation, createTeammate call, toast).
    - PENDO_IDS references (`PENDO_IDS.team.invite.modalContainer`, `.modalEmail`, `.modalRole`, `.modalCancel`, `.modalSubmit`).
    - The Cancel + Send invite Button layout in `<Group justify="flex-end">`.
    - Any TextInput / Select markup or props.
    - `data-pendo-id={PENDO_IDS.team.invite.modalContainer}` on the outer Modal.
    - `keepMounted={false}` (preserve as decorative; ESLint may flag this as redundant given Mantine's default — that is acceptable noise, not a gap).

    Final correctness check (read this back after applying edits): the file should import `useEffect, useMemo, useRef` from 'react' (not just `useMemo`); should NOT import `Title` from '@mantine/core'; should have a `prevOpenedRef` + `useEffect` calling `form.reset(defaultValues)`; the Mantine Modal's title prop should be the plain string `"Invite teammate"`; the file-header docblock should NOT contain the phrase "keepMounted={false} drops RHF state on close"; and the file should pass `npm run typecheck` with zero new errors and zero new warnings.
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && npm run typecheck 2>&1 | tail -20 && echo "---no Title import---" && grep -cE "import \\{[^}]*Title[^}]*\\} from '@mantine/core'" src/team/components/InviteTeammateModal.tsx && echo "---useEffect+useRef imported---" && grep -cE "import \\{[^}]*useEffect[^}]*useRef[^}]*\\} from 'react'|import \\{[^}]*useRef[^}]*useEffect[^}]*\\} from 'react'" src/team/components/InviteTeammateModal.tsx && echo "---prevOpenedRef present---" && grep -c "prevOpenedRef" src/team/components/InviteTeammateModal.tsx && echo "---form.reset(defaultValues) present---" && grep -cE "form\\.reset\\(defaultValues\\)" src/team/components/InviteTeammateModal.tsx && echo "---string title---" && grep -cE "title=\"Invite teammate\"" src/team/components/InviteTeammateModal.tsx && echo "---no Title JSX in title prop---" && grep -cE "title=\\{<Title" src/team/components/InviteTeammateModal.tsx && echo "---defaultValues memoized---" && grep -cE "useMemo\\(\\(\\) => \\(\\{[^}]*email:[^}]*workspaceRole:[^}]*\\}\\)" src/team/components/InviteTeammateModal.tsx && echo "---doc comment corrected---" && grep -cE "keepMounted=\\{false\\} drops RHF state on close" src/team/components/InviteTeammateModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `useEffect`, `useMemo`, `useRef` all imported from 'react' (grep: `grep -E "import \\{[^}]+\\} from 'react'" src/team/components/InviteTeammateModal.tsx` returns 1 match containing all three names; ordering tolerant)
    - `Title` is NOT imported from '@mantine/core' (grep: `grep -E "import \\{[^}]*\\bTitle\\b[^}]*\\} from '@mantine/core'" src/team/components/InviteTeammateModal.tsx | wc -l` is 0)
    - `prevOpenedRef` declared (grep: `grep -E "const prevOpenedRef = useRef\\(opened\\)" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - Open-transition useEffect calls `form.reset(defaultValues)` (grep: `grep -E "form\\.reset\\(defaultValues\\)" src/team/components/InviteTeammateModal.tsx` returns at least 1 match)
    - useEffect dep array contains opened, form, defaultValues — no `mode` (grep: `grep -E "\\[opened, form, defaultValues\\]" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - Modal title prop is a plain string (grep: `grep -E "title=\"Invite teammate\"" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - The JSX `<Title order={3}>Invite teammate</Title>` is GONE (grep: `grep -E "<Title order=\\{3\\}>Invite teammate" src/team/components/InviteTeammateModal.tsx | wc -l` is 0)
    - `defaultValues` is memoized at component scope (grep: `grep -E "const defaultValues: InviteFormValues = useMemo" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - The misleading line-16 doc comment is GONE (grep: `grep -cE "keepMounted=\\{false\\} drops RHF state on close" src/team/components/InviteTeammateModal.tsx` is 0)
    - Doc comment now references CR-01 / TaskFormModal lines 149-165 (grep: `grep -E "(CR-01|TaskFormModal.*149)" src/team/components/InviteTeammateModal.tsx` returns at least 1 match)
    - The Modal still carries `data-pendo-id={PENDO_IDS.team.invite.modalContainer}` (grep: `grep -E "PENDO_IDS\\.team\\.invite\\.modalContainer" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - createTeammate + findTeammateByEmail wiring preserved (grep: `grep -cE "(createTeammate|findTeammateByEmail)" src/team/components/InviteTeammateModal.tsx` is >= 2)
    - InviteFormSchema with .superRefine preserved (grep: `grep -E "superRefine" src/team/components/InviteTeammateModal.tsx` returns at least 1 match)
    - Toast copy 'Invite sent' preserved (grep: `grep -E "'Invite sent'" src/team/components/InviteTeammateModal.tsx` returns 1 match)
    - `npm run typecheck` exits 0 with no new errors and no `Title` unused-import warning
    - `npm run lint` exits 0 (if eslint binary on PATH; mirrors Phase 5 precedent if not)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Reopening the modal after a submit OR cancel shows blank defaults; no h3-in-h2 console error on modal mount; no unused Title import; doc comment accurately describes the reset mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Correct misleading patterns-established entry in 05-04-SUMMARY.md</name>
  <files>.planning/phases/05-team-help-polish/05-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-team-help-polish/05-04-SUMMARY.md (line 52 — the entry to correct)
    - .planning/debug/05-invite-modal-h3-in-h2.md Evidence section (timestamp 2026-05-15 — comparison of actual Phase 4 precedent in TaskFormModal:273 vs the SUMMARY's mistaken claim)
    - src/tasks/components/TaskFormModal.tsx:273 (the actual Phase 4 precedent — `title={mode === 'create' ? 'New task' : 'Edit task'}` — a plain string)
  </read_first>
  <action>
    Update line 52 of `.planning/phases/05-team-help-polish/05-04-SUMMARY.md`. The current entry reads:

      "Modal title as `<Title order={3}>` JSX element (not string prop) per Phase 4 modal-nesting fix"

    This is the OPPOSITE of the actual Phase 4 precedent (TaskFormModal:273 passes a plain string, as do ResetDemoDataModal:128 and DeleteConfirmModal:48). The misstatement is what led Plan 04 to introduce the h3-in-h2 bug. Replace it with an accurate entry:

      "Modal title as plain string (NOT a `<Title>` JSX wrapper) — Mantine's ModalBaseTitle renders the title slot as `<Box component='h2'>` unconditionally, so wrapping in `<Title order={3}>` produces invalid `<h2><h3>...</h3></h2>` nesting (React 19 hydration error). Phase 4 precedent: TaskFormModal:273, ResetDemoDataModal:128, DeleteConfirmModal:48 all pass plain strings. Plan 08 corrected this entry after the original Plan 04 SUMMARY misstated the precedent."

    Keep the surrounding list structure (it is a bullet under the `patterns-established:` YAML list around lines 50-52). Preserve the YAML indentation level. If the list-style is dashes, keep dashes; if it is plain text, preserve.

    DO NOT edit any other line in 05-04-SUMMARY.md. The frontmatter, the rest of the body, the metrics — all stay.

    NOTE: This is a documentation correction, not a code change. It does not need a typecheck. It DOES warrant a separate commit from Task 1 because mixing source-code fixes with planning-doc corrections in a single commit muddies git history.
  </action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && echo "---wrong entry gone---" && grep -cE "Modal title as <Title order=\\{3\\}> JSX element \\(not string prop\\)" .planning/phases/05-team-help-polish/05-04-SUMMARY.md && echo "---corrected entry present---" && grep -cE "Modal title as plain string \\(NOT a" .planning/phases/05-team-help-polish/05-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The original mistaken entry is GONE (grep: `grep -cE "Modal title as <Title order=\\{3\\}> JSX element \\(not string prop\\)" .planning/phases/05-team-help-polish/05-04-SUMMARY.md` is 0)
    - The corrected entry is present (grep: `grep -cE "Modal title as plain string \\(NOT a" .planning/phases/05-team-help-polish/05-04-SUMMARY.md` is >= 1)
    - The corrected entry references the Phase 4 precedent files (grep: `grep -E "(TaskFormModal:273|ResetDemoDataModal:128|DeleteConfirmModal:48)" .planning/phases/05-team-help-polish/05-04-SUMMARY.md` returns at least 1 match)
    - The corrected entry references React 19 hydration / `<h2><h3>` nesting (grep: `grep -E "(React 19|<h2><h3>|hydration)" .planning/phases/05-team-help-polish/05-04-SUMMARY.md` returns at least 1 match)
    - No other lines in 05-04-SUMMARY.md changed (developer can confirm via `git diff .planning/phases/05-team-help-polish/05-04-SUMMARY.md` showing exactly one bullet replaced)
  </acceptance_criteria>
  <done>05-04-SUMMARY.md's patterns-established list now accurately describes the modal-title precedent — future Phase 5+ modal authors will follow the string-title rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> form -> repo | Pre-existing boundary (email field accepts arbitrary user input, validated by Zod). Plan 08 does NOT change the trust boundary — it changes lifecycle (when form state resets) and DOM shape (title slot). The InviteFormSchema validation + .superRefine dedupe + lowercase-normalization on createTeammate all remain verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Form state leak between opens (Gap 2 itself) | mitigate | This IS the gap. The fix (prevOpenedRef + form.reset on open transition) is the mitigation. Without the fix, a user filling email A in attempt 1 then opening the modal again could accidentally re-submit email A even when intending to type email B (data-integrity issue for a demo-data SaaS surface — accepting the wrong invite). |
| T-05-08-02 | Information Disclosure | Cross-open form state retention could expose previous email value to a subsequent user on shared machines | accept | PROJECT.md "Out of Scope" — Halo is fabricated demo data; shared-machine privacy is not a project concern. The fix happens to mitigate this incidentally but it is not the load-bearing rationale. |
| T-05-08-03 | DoS | useEffect dependency churn from non-stable defaultValues could re-fire the reset effect every render | mitigate | EDIT 2 wraps defaultValues in useMemo with an empty dep array — the reference is stable across all renders for this component. The effect runs only on false->true `opened` transitions per the prevOpenedRef guard. |
| T-05-08-04 | Information Disclosure | React 19 hydration error (Gap 3) leaks `<h3> cannot be a child of <h2>` to the browser console | mitigate | EDIT 4 (string title prop) eliminates the malformed DOM at source. No nested heading rendered; no console error fired. |
| T-05-08-05 | Spoofing | Not applicable | n/a | No identity assertion changes. |
| T-05-08-06 | Repudiation | Not applicable | n/a | Demo data; no audit requirement. |
| T-05-08-07 | Elevation of Privilege | Not applicable | n/a | Form lifecycle change does not affect any privilege boundary. |

All threats are either directly mitigated by the fix or explicitly accepted on demo-data grounds.
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint` (if eslint binary on PATH), `npm run build` all exit 0.
- Static guarantees:
  - `grep -E "<Title order=\\{3\\}>Invite teammate" src/team/components/InviteTeammateModal.tsx | wc -l` is 0.
  - `grep -E "title=\"Invite teammate\"" src/team/components/InviteTeammateModal.tsx | wc -l` is 1.
  - `grep -E "prevOpenedRef" src/team/components/InviteTeammateModal.tsx | wc -l` is >= 2 (declaration + .current usage).
  - `grep -E "form\\.reset\\(defaultValues\\)" src/team/components/InviteTeammateModal.tsx | wc -l` is >= 1.
  - `grep -E "import \\{[^}]*Title[^}]*\\} from '@mantine/core'" src/team/components/InviteTeammateModal.tsx | wc -l` is 0.
  - `grep -cE "Modal title as <Title order=\\{3\\}> JSX element \\(not string prop\\)" .planning/phases/05-team-help-polish/05-04-SUMMARY.md` is 0 (mistaken entry removed).
- Manual smoke (after execution, developer runs `npm run dev`):
  - Sign in -> /app/team -> click "Invite teammate" -> open DevTools Console -> confirm NO message containing "In HTML, <h3> cannot be a child of <h2>". Close DevTools.
  - Fill email "alice@example.com", role "Admin" -> submit -> watch the new row appear and the green "Invite sent" toast fire -> click "Invite teammate" again -> the email field is EMPTY, the role is "Member".
  - Click "Invite teammate" -> fill email "bob@example.com", role "Viewer" -> click Cancel (do NOT submit) -> click "Invite teammate" again -> the email field is EMPTY, the role is "Member".
  - Confirm the modal title still reads "Invite teammate" and visually matches the styling of TaskFormModal's title (size and weight). Right-click the title -> Inspect -> confirm DOM is `<h2 class="...ModalTitle">Invite teammate</h2>` (no nested h3).
  - Confirm Plan 07's fix (Gap 1+4) and Plan 08's fix (Gap 2+3) are independent: even if Plan 07 hasn't landed yet, Plan 08's modal-only fix still verifies cleanly via the existing seeded /app/team page after a Reset demo data cycle (Test 10 path).
</verification>

<success_criteria>
- Gap 2 closed: Invite teammate modal opens with blank defaults on every reopen (after submit AND after cancel).
- Gap 3 closed: No "In HTML, <h3> cannot be a child of <h2>" console error on modal mount.
- Phase 4 CR-01 reset pattern ported verbatim minus the `mode === 'create'` guard (single-mode component).
- Title import removed from @mantine/core; no unused-import lint warning.
- 05-04-SUMMARY.md patterns-established list no longer misstates Phase 4's modal-title precedent.
- No regressions in typecheck / lint / build.
- All preserved behavior (Zod validation, .superRefine dedupe, createTeammate call, toast copy, PENDO_IDS wiring) verifiably intact via static grep.
</success_criteria>

<output>
After completion, create `.planning/phases/05-team-help-polish/05-08-SUMMARY.md` documenting:
- Decision to bundle Gaps 2 + 3 in one plan (same file, surgical fixes, atomic-commit hygiene served by combined plan with separate task commits).
- Exact line-range diff for InviteTeammateModal.tsx (the five edits applied — imports, defaultValues memo, prevOpenedRef + useEffect, title prop, doc comment).
- The two commits that should land for this plan: (1) "fix(05-08): port CR-01 reset pattern + string title in InviteTeammateModal" covering Task 1, (2) "docs(05-08): correct 05-04-SUMMARY patterns-established modal-title entry" covering Task 2.
- Confirmation that no behavioral regressions to other Invite-flow paths (validation messages, duplicate-email dedupe, toast copy, PENDO_IDS targeting) were introduced.
- The decorative `keepMounted={false}` on line 111 was deliberately left in place (a separate cleanup if anyone wants to remove it; out of scope for this gap closure).
- Any deviations from this plan and the rationale.
</output>
