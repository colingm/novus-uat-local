---
phase: quick-260518-f3f
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/halo-logo-dark.png
  - src/routes/app/AppLayout.tsx
  - src/routes/public/Landing.tsx
  - full-logo-dark-mode.png
autonomous: true
requirements:
  - QUICK-260518-f3f-01
tags: [logo, dark-mode, branding, mantine, pendo, AppLayout, Landing]

must_haves:
  truths:
    - "When user is in dark mode, the header logo and landing hero logo render the light/white wordmark variant"
    - "When user is in light mode, the header logo and landing hero logo render the original dark wordmark variant"
    - "Logo swap is driven by Mantine's resolved color scheme (not raw color-scheme state, never the string 'auto')"
    - "Pendo guide targeting on the header logo still resolves — data-pendo-id={PENDO_IDS.topbar.logo} remains on the Image element"
    - "TypeScript compiles cleanly with zero new errors"
  artifacts:
    - path: "public/halo-logo-dark.png"
      provides: "Dark-mode header/landing logo asset (white wordmark on transparent bg)"
    - path: "src/routes/app/AppLayout.tsx"
      provides: "Header logo conditionally renders /halo-logo.png or /halo-logo-dark.png via useComputedColorScheme"
      contains: "useComputedColorScheme"
    - path: "src/routes/public/Landing.tsx"
      provides: "Landing hero logo conditionally renders /halo-logo.png or /halo-logo-dark.png via useComputedColorScheme"
      contains: "useComputedColorScheme"
  key_links:
    - from: "src/routes/app/AppLayout.tsx Image src"
      to: "useComputedColorScheme('light') return value"
      via: "ternary expression on computed scheme"
      pattern: "useComputedColorScheme"
    - from: "src/routes/public/Landing.tsx Image src"
      to: "useComputedColorScheme('light') return value"
      via: "ternary expression on computed scheme"
      pattern: "useComputedColorScheme"
    - from: "src/routes/app/AppLayout.tsx Image"
      to: "PENDO_IDS.topbar.logo"
      via: "data-pendo-id attribute"
      pattern: "data-pendo-id=\\{PENDO_IDS\\.topbar\\.logo\\}"
---

<objective>
Add a dark-mode variant for the Halo header and landing logo. Copy the new dark-mode PNG into `public/`, then update both `AppLayout.tsx` (header, h=32) and `Landing.tsx` (hero, h=56) to swap the `Image` `src` between `/halo-logo.png` (light) and `/halo-logo-dark.png` (dark) using Mantine's `useComputedColorScheme('light')` hook. Remove the original dark-mode PNG from the repo root once copied.

Purpose: The existing light-mode logo (wired in quick task 260518-eta) is unreadable when the user toggles dark mode via Settings (Phase 4) or via OS preference. Mantine is already the source of truth for color scheme in this app; the logo swap must follow that same state, not a separate `prefers-color-scheme` CSS path.

Output: A second logo asset in `public/`, two updated route components with conditional `Image src`, and the source PNG removed from the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/quick/260518-eta-wire-up-logo-pngs-as-favicon-and-header-/260518-eta-SUMMARY.md
@src/routes/app/AppLayout.tsx
@src/routes/public/Landing.tsx

<interfaces>
<!-- Key Mantine API the executor will use. Mantine 9 ships these from @mantine/core. -->

From @mantine/core (already imported in both target files):
```typescript
// useComputedColorScheme resolves 'auto' → concrete 'light' | 'dark' based on
// MantineProvider state + OS preference. Requires a default arg used during SSR
// or before the inline ColorSchemeScript runs. Returns 'light' | 'dark' — never 'auto'.
export function useComputedColorScheme(
  defaultValue?: 'light' | 'dark',
  options?: { getInitialValueInEffect?: boolean }
): 'light' | 'dark';
```

DO NOT use `useMantineColorScheme().colorScheme` — that returns `'light' | 'dark' | 'auto'` and `'auto'` is not a paintable value for an image src.

Light asset: `/halo-logo.png` (already in `public/`, committed)
Dark asset: `/halo-logo-dark.png` (this plan creates it in `public/`)
</interfaces>

<source_assets>
Dark-mode PNG source (untracked in git, at repo root):
- `/Users/colin.maxfield/testspace/test-sites/halo-app/full-logo-dark-mode.png` (113 KB, verified to exist)

Because the source is untracked, use plain `cp` (not `git mv`) to copy it into `public/halo-logo-dark.png`. Then delete the original at the repo root with `rm` so it does not linger as a stray untracked file. This mirrors the pattern from quick task 260518-eta.
</source_assets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy dark-mode PNG into public/ and remove source</name>
  <files>public/halo-logo-dark.png, full-logo-dark-mode.png</files>
  <action>Copy `/Users/colin.maxfield/testspace/test-sites/halo-app/full-logo-dark-mode.png` to `/Users/colin.maxfield/testspace/test-sites/halo-app/public/halo-logo-dark.png` using `cp` (the source is untracked, so `git mv` is wrong — use plain `cp` exactly as quick task 260518-eta did for the light asset). After the copy succeeds and the file is verified at the destination, delete the original at `/Users/colin.maxfield/testspace/test-sites/halo-app/full-logo-dark-mode.png` with `rm`. Do not rename the existing `public/halo-logo.png` — it remains the canonical light asset.</action>
  <verify>
    <automated>test -f /Users/colin.maxfield/testspace/test-sites/halo-app/public/halo-logo-dark.png && ! test -f /Users/colin.maxfield/testspace/test-sites/halo-app/full-logo-dark-mode.png && test -f /Users/colin.maxfield/testspace/test-sites/halo-app/public/halo-logo.png</automated>
  </verify>
  <done>`public/halo-logo-dark.png` exists, `public/halo-logo.png` still exists, and the repo-root `full-logo-dark-mode.png` has been removed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useComputedColorScheme logo swap in AppLayout and Landing</name>
  <files>src/routes/app/AppLayout.tsx, src/routes/public/Landing.tsx</files>
  <action>Update both files to swap the `Image` `src` based on Mantine's resolved color scheme.

In `src/routes/app/AppLayout.tsx`:
1. Extend the existing `@mantine/core` named import to include `useComputedColorScheme` (add it to the existing import list — do not introduce a second import statement from `@mantine/core`).
2. Inside the `AppLayout` component, near the other hook calls (`useLocation`, `useNavigate`, the `useAuthStore` selectors), add `const colorScheme = useComputedColorScheme('light')`. The `'light'` default is the conventional SSR-safe fallback; pass it even though the app is a client-only SPA whose inline `ColorSchemeScript` runs first.
3. Change the header `Image` `src` from the literal `"/halo-logo.png"` to the ternary `colorScheme === 'dark' ? '/halo-logo-dark.png' : '/halo-logo.png'`.
4. Preserve every other attribute on that `Image` element verbatim — especially `data-pendo-id={PENDO_IDS.topbar.logo}` (Pendo guide targeting depends on it), plus `alt`, `h={32}`, `w="auto"`, `fit="contain"`.

In `src/routes/public/Landing.tsx`:
1. Extend the existing `@mantine/core` named import to include `useComputedColorScheme`.
2. Inside the `Landing` component (before the `return`), add `const colorScheme = useComputedColorScheme('light')`.
3. Change the hero `Image` `src` from `"/halo-logo.png"` to the ternary `colorScheme === 'dark' ? '/halo-logo-dark.png' : '/halo-logo.png'`.
4. Preserve `alt`, `h={56}`, `w="auto"`, `fit="contain"`. Do not add a `data-pendo-id` (Landing logo has none today and is out of scope).

Do NOT use `useMantineColorScheme().colorScheme` — that returns `'auto'` when the user has not explicitly picked a scheme, and `'auto'` is not a paintable image src. Do NOT replace `<Image>` with `<picture>` or with a `prefers-color-scheme` CSS approach — Mantine's color-scheme state is already the source of truth and must drive the swap so the Phase 4 Settings toggle controls the logo.</action>
  <verify>
    <automated>cd /Users/colin.maxfield/testspace/test-sites/halo-app && grep -q "useComputedColorScheme" src/routes/app/AppLayout.tsx && grep -q "useComputedColorScheme" src/routes/public/Landing.tsx && grep -q "/halo-logo-dark.png" src/routes/app/AppLayout.tsx && grep -q "/halo-logo-dark.png" src/routes/public/Landing.tsx && grep -q "data-pendo-id={PENDO_IDS.topbar.logo}" src/routes/app/AppLayout.tsx && ! grep -q "useMantineColorScheme" src/routes/app/AppLayout.tsx && ! grep -q "useMantineColorScheme" src/routes/public/Landing.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Both files import `useComputedColorScheme` from `@mantine/core`, call it with default `'light'`, and use a ternary on the result to pick between `/halo-logo.png` and `/halo-logo-dark.png` for the `Image` `src`. `data-pendo-id={PENDO_IDS.topbar.logo}` is intact on the AppLayout header logo. `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `public/halo-logo-dark.png` exists; `public/halo-logo.png` still exists.
- Repo root has no `full-logo-dark-mode.png` lingering.
- `src/routes/app/AppLayout.tsx` and `src/routes/public/Landing.tsx` both call `useComputedColorScheme('light')` and use its return in a ternary for the `Image` `src`.
- `AppLayout.tsx` header `Image` still carries `data-pendo-id={PENDO_IDS.topbar.logo}`.
- Neither file uses `useMantineColorScheme().colorScheme` for the src decision.
- `npx tsc --noEmit` passes with zero errors.
</verification>

<success_criteria>
- Toggling the app between light and dark color scheme swaps the header logo and the landing hero logo between the two PNG variants (verifiable by visiting `/` and `/app` and flipping the Phase 4 Settings color-scheme toggle, or by hitting OS-level dark mode with `auto`).
- Pendo's `[data-pendo-id="topbar.logo"]` selector resolves to the header `<img>` in both light and dark modes.
- No new TypeScript errors and no behavioral regression on light mode (default scheme still renders `/halo-logo.png`).
</success_criteria>

<output>
After completion, create `.planning/quick/260518-f3f-add-dark-mode-header-logo-variant/260518-f3f-SUMMARY.md` documenting the asset added, the two route components updated, the `useComputedColorScheme` choice rationale, and the Pendo selector preservation.
</output>
