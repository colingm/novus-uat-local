---
phase: quick-260825-cn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - src/router.tsx
  - src/routes/app/AppLayout.tsx
  - src/routes/public/Landing.tsx
  - src/routes/public/SignInPage.tsx
  - src/routes/public/PrimitivesSandbox.tsx
  - src/routes/public/signup/Step1AccountPage.tsx
  - src/routes/public/signup/SignupShell.tsx
  - .github/workflows/deploy-pages.yml
autonomous: true
requirements: [QUICK-260825-cn1]

user_setup:
  - service: github-pages
    why: "Repo Pages source must be switched to the Actions deployment path; `actions/deploy-pages` fails if the repo is still set to 'Deploy from a branch'. No CLI/API step the executor can safely run for the user."
    dashboard_config:
      - task: "Set Pages source to 'GitHub Actions'"
        location: "github.com/colingm/novus-uat-local -> Settings -> Pages -> Build and deployment -> Source"
      - task: "Confirm the repo is Public (project Pages on a private repo requires a paid plan)"
        location: "github.com/colingm/novus-uat-local -> Settings -> General -> Danger Zone"

must_haves:
  truths:
    - "A local `npm run build` emits `dist/index.html` whose asset and favicon URLs are origin-root (`/assets/...`, `/favicon.png`), unchanged from today."
    - "A build with `GITHUB_ACTIONS=true` emits `dist/index.html` whose asset and favicon URLs are prefixed `/novus-uat-local/`."
    - "Every build (local or CI) also emits `dist/404.html` byte-identical to `dist/index.html`, so a hard refresh on `https://colingm.github.io/novus-uat-local/app/reports` boots the SPA and renders Reports."
    - "No image `src` or anchor `href` literal in `src/` points at an origin-root path, so logos load and public links resolve under the `/novus-uat-local/` subpath."
    - "`npm run dev` still serves on port 3030 with routes at `/`, `/signup`, `/app` and the chat proxy still mounted at `/api/chat/stream` — zero dev-behavior change."
    - "A push to `main` runs `.github/workflows/deploy-pages.yml`, which builds and publishes `dist/` through GitHub's Actions Pages deployment with no repository secrets present in the build."
  artifacts:
    - "vite.config.ts — `base` gate plus a build-only 404.html emit plugin"
    - "src/router.tsx — `createBrowserRouter(routes, { basename: import.meta.env.BASE_URL })`"
    - ".github/workflows/deploy-pages.yml — Actions-native Pages deploy"
    - "dist/404.html — build output, byte-identical copy of dist/index.html"
  key_links:
    - "`base` in vite.config.ts -> `import.meta.env.BASE_URL` -> router basename AND every asset/href literal. Single source of truth: changing the deployment subpath must require editing vite.config.ts only, never src/."
    - "`dist/404.html` -> GitHub Pages unmatched-path handling. This is the ONLY deep-link mechanism available on Pages (no rewrite rules, no `_redirects`). If it stops being emitted, every deep link and every hard refresh dies."
    - "Repo Pages source = 'GitHub Actions' -> `actions/deploy-pages`. If the repo is still on 'Deploy from a branch', the workflow goes green while nothing publishes."
---

<objective>
Make Halo deployable to GitHub Pages as a static SPA served from the public repo
`colingm/novus-uat-local` at `https://colingm.github.io/novus-uat-local/`.

Purpose: give Pendo folks a shareable URL for the demo surface instead of requiring
`npm run dev` on a local machine. Halo is already frontend-only with localStorage
persistence, so nothing about the app's architecture has to change — only the
assumption that it is served from an origin root.

Output: a base-path-aware build (`base` + router `basename` + BASE_URL-relative
literals), a `dist/404.html` deep-link fallback, and a GitHub Actions Pages workflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@vite.config.ts
@src/router.tsx
@src/routes/public/Landing.tsx
@src/routes/app/AppLayout.tsx
</context>

<scope_notes>
Facts established during planning — treat as given, do not re-derive:

1. **Target is confirmed.** `git remote -v` resolves to `git@github.com:colingm/novus-uat-local.git`, so the
   project-page base is exactly `/novus-uat-local/`.
2. **`.github/` does not exist yet.** The workflow file needs its parent directories created.
3. **`public/` holds four files:** `favicon.png`, `halo-logo.png`, `halo-logo-dark.png`, `vite.svg`.
   `vite.svg` is referenced nowhere in `src/` (verified by grep) — leave it alone.
4. **Vite rewrites root-relative URLs inside `index.html` automatically** when `base` is set.
   `href="/favicon.png"` and `src="/src/main.tsx"` need no manual edit. **Do not touch `index.html`.**
5. **The Pendo snippet stays exactly as-is.** Its agent `src` is an absolute `https://` URL, unaffected by `base`.
6. **No new npm dependencies.** Nothing in this plan installs a package, so no package-legitimacy audit
   applies and `npm ci` in CI resolves purely from the committed `package-lock.json`.
7. **`chatProxyPlugin` is already build-inert.** It only registers a `configureServer` hook, so it
   contributes nothing to `vite build` and needs no API key in CI. Leave it untouched.

**Discovered during planning — root-relative route `href`s (NOT in the original brief).**
Six `<Anchor href="/...">` sites render real `<a href>` DOM attributes, which are full-page
browser navigations and therefore bypass the React Router `basename` entirely. On the deployed
site `href="/signin"` would send the browser to `colingm.github.io/signin` — outside the project
path, so it would hit GitHub's own 404 page, not even Halo's `404.html`. These are broken *by*
the basename change and must be fixed for the deploy to work:

| File | Line | Literal |
|---|---|---|
| `src/routes/public/Landing.tsx` | 30 | `href="/signin"` |
| `src/routes/public/Landing.tsx` | 31 | `href="/signup"` |
| `src/routes/public/SignInPage.tsx` | 133 | `href="/signup"` |
| `src/routes/public/PrimitivesSandbox.tsx` | 54 | `href="/"` |
| `src/routes/public/signup/Step1AccountPage.tsx` | 122 | `href="/signin"` |
| `src/routes/public/signup/SignupShell.tsx` | 89 | `href="/signin"` |

All `navigate(...)` and `<Navigate to>` / `<Link to>` call sites are router-relative and
**already handled** by the `basename` — leave every one of them alone.

**Deliberately left broken (user-accepted).** `src/chat/claudeClient.ts:58` fetches
`/api/chat/stream`, a Vite *dev-server* plugin with no production equivalent. Chat WILL fail on
the deployed site. Do NOT hide, gate, flag, or otherwise modify `ChatLauncher`
(`src/chat/ChatLauncher.tsx`, mounted at `src/routes/app/AppLayout.tsx:242`) and do NOT change
that fetch path. Documenting the limitation in the SUMMARY is fine; changing behavior is not.

**Accepted caveat to record, not to solve.** GitHub Pages serves `404.html` with an HTTP 404
*status code*. Deep links will boot and render correctly, but the response status stays 404.
This is cosmetic for a demo app and does not affect Pendo (the agent runs client-side once the
SPA boots). Do not attempt to work around it.

**`npm run preview` is not a faithful Pages simulation.** `vite preview` has its own SPA
index.html fallback, so it will serve deep links successfully whether or not `404.html` exists.
Use `preview` to eyeball the subpath build; rely on the file-level assertions in `<verify>` to
prove the fallback artifact is actually there.
</scope_notes>

<tasks>

<!-- planner-discipline-allow: createHashRouter -->
<!-- Load-bearing: FND-03 requires naming the rejected router factory so the executor cannot
     substitute it. The Task 1 gate greps only non-comment lines of src/router.tsx, and that
     file's existing FND-03 comment block already names it, so comment text cannot trip it. -->
<task type="tracer">
  <name>Task 1: Serve the whole app from /novus-uat-local/ — build config + router basename + 404 fallback</name>
  <files>vite.config.ts, src/router.tsx</files>
  <reversibility rating="reversible">Two localized edits to config and one router call; `git revert` restores origin-root serving with no data or schema consequences.</reversibility>
  <action>
Thinnest end-to-end slice: one production build that boots under a subpath. Two files.

**vite.config.ts — `base` gate.** Inside the existing `defineConfig(({ mode }) => ...)` return
object, add `base: process.env.GITHUB_ACTIONS ? '/novus-uat-local/' : '/'` alongside the
existing `plugins` and `server` keys. `process` already typechecks in this file (`loadEnv` uses
`process.cwd()`), so no new import is needed. Read the gate from `process.env` directly rather
than the `env` object returned by `loadEnv` — `loadEnv` with an empty prefix merges `.env`
files into its result, and the deployment base must depend only on the real CI environment.
Leave `plugins: [react(), chatProxyPlugin(env.ANTHROPIC_API_KEY)]` and `server: { port: 3030 }`
byte-identical.

**vite.config.ts — 404.html emit plugin.** Add a second plugin factory next to
`chatProxyPlugin`, named `pagesFallbackPlugin`, returning a plugin object with
`name: 'halo-pages-404-fallback'`, `apply: 'build'`, a `configResolved` hook that captures the
resolved `root` and `build.outDir`, and a `closeBundle` hook that copies `index.html` to
`404.html` inside the resolved out dir via `copyFileSync` from `node:fs`, with paths joined by
`resolve` from `node:path`. Type the `configResolved` parameter with the inline-import style
already used on line 30 of this file (`config: import('vite').ResolvedConfig`) so the import
list at the top stays unchanged. Append the factory call to the `plugins` array AFTER
`chatProxyPlugin(...)`.

Chose the plugin over an npm `postbuild` script deliberately: it is cross-platform (no `cp`
dependency), it fires for a bare `vite build` and not just `npm run build`, and it lives
directly beside the `base` value it is coupled to. Tradeoff accepted: the copy is invisible to
anyone reading `package.json` alone, so the plugin's own doc comment must state that
`dist/404.html` is the GitHub Pages deep-link fallback and that Pages has no rewrite mechanism.

`closeBundle` is the chosen hook because it fires once, after every output file is on disk, so
the copy is unambiguously byte-for-byte. `configResolved` is required because `build.outDir`
may be configured relative to `root`.

**src/router.tsx — basename.** Line 65 currently reads
`export const router = createBrowserRouter([` with no options argument. Pass a second argument
`{ basename: import.meta.env.BASE_URL }` after the closing bracket of the route array.
`import.meta.env.BASE_URL` is `/` in dev and `/novus-uat-local/` in a Pages build; React Router
7 accepts a basename with a trailing slash. Keep `createBrowserRouter` — History API routing is
locked by FND-03 and Pendo's SPA route auto-detection depends on it.

The FND-03 comment block ending at line 43 currently claims "The Vite dev server's default SPA
fallback serves index.html on every path, so refreshing on `/app/reports` works without
server-side routing logic." That sentence is now misleading for production. Amend that FND-03
paragraph to state that the router is mounted under `import.meta.env.BASE_URL` and that on a
static host the emitted `404.html` copy of `index.html` is what makes a deep-link refresh work.
Leave the Phase 1/2/3/6 narrative paragraphs above it untouched.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; rm -rf dist &amp;&amp; GITHUB_ACTIONS=true npm run build &amp;&amp; grep -q '/novus-uat-local/assets/' dist/index.html &amp;&amp; grep -q '/novus-uat-local/favicon.png' dist/index.html &amp;&amp; cmp -s dist/index.html dist/404.html &amp;&amp; [ "$(grep -c 'basename: import.meta.env.BASE_URL' src/router.tsx)" = "1" ] &amp;&amp; [ "$(grep -vE '^\s*(\*|//|/\*)' src/router.tsx | grep -c createHashRouter)" = "0" ] &amp;&amp; rm -rf dist &amp;&amp; npm run build &amp;&amp; grep -q 'src="/assets/' dist/index.html &amp;&amp; cmp -s dist/index.html dist/404.html &amp;&amp; grep -q 'port: 3030' vite.config.ts</automated>
  </verify>
  <done>A CI-flavored build emits base-prefixed asset and favicon URLs; a plain build emits origin-root URLs; both emit `dist/404.html` byte-identical to `dist/index.html`; the router mounts under `import.meta.env.BASE_URL` with `createBrowserRouter` intact (the `createHashRouter` gate greps only non-comment lines, because the FND-03 comment legitimately names it); dev server port is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase every origin-root URL literal in src/ onto import.meta.env.BASE_URL</name>
  <files>src/routes/app/AppLayout.tsx, src/routes/public/Landing.tsx, src/routes/public/SignInPage.tsx, src/routes/public/PrimitivesSandbox.tsx, src/routes/public/signup/Step1AccountPage.tsx, src/routes/public/signup/SignupShell.tsx</files>
  <action>
Vite rewrites root-relative URLs in `index.html` but never string literals in TSX. Nine literals
across six files must become BASE_URL-relative. `import.meta.env.BASE_URL` always ends in a
slash, so concatenate the path segment directly — never add a second slash.

**Asset literals (3):**
- `src/routes/app/AppLayout.tsx:121-122` — the `colorScheme === "dark"` ternary feeding
  `<Image src>`: `"/halo-logo-dark.png"` and `"/halo-logo.png"`.
- `src/routes/public/Landing.tsx:19` — both branches of the one-line
  `colorScheme === 'dark' ? ... : ...` ternary.

**Route href literals (6)** — see the table in `<scope_notes>`. These render real `<a href>`
attributes, so they are full-page navigations that never see the router basename:
`Landing.tsx:30` and `:31`, `SignInPage.tsx:133`, `PrimitivesSandbox.tsx:54`,
`Step1AccountPage.tsx:122`, `SignupShell.tsx:89`.

For the five that carry a path segment, use a template literal interpolating
`import.meta.env.BASE_URL` followed by the bare segment (`signin` / `signup`) with no leading
slash. For `PrimitivesSandbox.tsx:54`, which links to the site root, pass
`import.meta.env.BASE_URL` on its own — no template literal, no trailing segment.

Preserve each file's existing quote style (`AppLayout.tsx` uses double quotes; the
`src/routes/public/` files use single quotes) and preserve every `pendoId` /
`data-pendo-id` prop verbatim — the Pendo selector contract is unaffected by this change and
must not shift.

Keep these as `<a href>` navigations rather than converting to React Router `<Link>`. The
BASE_URL prefix is a behavior-preserving one-line change per site; converting to `<Link>` would
alter navigation semantics (client-side transition instead of a document load), touch the
`Anchor` primitive's polymorphic typing, and risk the Pendo markup contract — all outside a
quick task. Note the deferral in the SUMMARY.

Do NOT touch `src/chat/claudeClient.ts` — its `/api/chat/stream` fetch is deliberately left
broken in production (see `<scope_notes>`). Do NOT touch any `navigate(...)`, `<Navigate to>`,
or `<Link to>` call site: those are router-relative and the basename already handles them.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; [ "$(grep -rnE "['\"\`]/(halo-logo|halo-logo-dark|favicon|vite)\.(png|svg|ico)" src/ | wc -l | tr -d ' ')" = "0" ] &amp;&amp; [ "$(grep -rnE 'href=\{?["'"'"'\`]/' src/ | wc -l | tr -d ' ')" = "0" ] &amp;&amp; [ "$(grep -rc 'import.meta.env.BASE_URL' src/routes/public/Landing.tsx src/routes/app/AppLayout.tsx src/routes/public/SignInPage.tsx src/routes/public/PrimitivesSandbox.tsx src/routes/public/signup/Step1AccountPage.tsx src/routes/public/signup/SignupShell.tsx | grep -c ':0$')" = "0" ] &amp;&amp; grep -q 'api/chat/stream' src/chat/claudeClient.ts &amp;&amp; rm -rf dist &amp;&amp; GITHUB_ACTIONS=true npm run build &amp;&amp; grep -rq 'novus-uat-local' dist/assets/ &amp;&amp; cmp -s dist/index.html dist/404.html</automated>
  </verify>
  <done>Zero origin-root public-asset literals and zero origin-root `href=` literals remain anywhere in `src/`; all six touched files reference `import.meta.env.BASE_URL` at least once; the chat fetch path is still `/api/chat/stream` (untouched); a CI-flavored build substitutes the base into the JS bundle and still emits the 404 fallback.</done>
</task>

<!-- planner-discipline-allow: gh-pages -->
<!-- planner-discipline-allow: secrets. -->
<!-- Load-bearing: the executor must be told by name which publish mechanism and which
     third-party action to reject, and that no `secrets.*` reference belongs in the workflow.
     Both literals are negative-greped in .github/workflows/deploy-pages.yml only — never in
     this plan — and the action body carries an explicit instruction to keep them out of the
     YAML's comments. -->
<task type="auto">
  <name>Task 3: Add the GitHub Actions Pages deployment workflow</name>
  <files>.github/workflows/deploy-pages.yml</files>
  <action>
Create `.github/workflows/deploy-pages.yml` (the `.github/` tree does not exist — create it).
Use GitHub's modern Actions-native Pages flow. Do NOT publish to a `gh-pages` branch and do NOT
add `peaceiris/actions-gh-pages` or any other third-party action; first-party `actions/*` only.

Shape:
- `name` describing the workflow, e.g. "Deploy to GitHub Pages".
- `on`: `push` restricted to `branches: [main]`, plus `workflow_dispatch` for manual runs.
- Top-level `permissions`: `contents: read`, `pages: write`, `id-token: write`. Nothing wider —
  the deploy needs OIDC token minting and Pages write, never repo write.
- `concurrency`: group `"pages"` with `cancel-in-progress: false`, so a queued deploy finishes
  rather than being killed mid-publish by a newer push.
- A single `deploy` job on `runs-on: ubuntu-latest` declaring
  `environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }`.
- Steps in order: `actions/checkout@v4`; `actions/setup-node@v4` with `node-version: '22'` and
  `cache: 'npm'` (Node 22 matches the local toolchain — TypeScript 6 and `@types/node` 26);
  `npm ci`; `npm run build`; `actions/configure-pages@v5`;
  `actions/upload-pages-artifact@v3` with `path: ./dist`; and
  `actions/deploy-pages@v4` carrying `id: deployment` so the environment URL output resolves.

`GITHUB_ACTIONS` is set to `true` by the runner automatically, so `npm run build` picks up the
`/novus-uat-local/` base with no explicit env wiring.

This workflow requires no repository secrets: add no `env:` block and no `secrets.*` reference
anywhere in the file. The build is published to a world-readable origin, so any build-time
value inlined by Vite would be public — the `chatProxyPlugin` contributes nothing to
`vite build` and needs no key. Add no comment in this file naming any credential.

Include a short leading comment stating that the repo's Pages source must be set to
"GitHub Actions" in Settings -> Pages for `actions/deploy-pages` to publish, and that
`dist/404.html` (emitted by the Vite plugin in Task 1) is what makes SPA deep links work.

Comment discipline for this file: the `<verify>` gate negative-greps the branch-publish
mechanism this workflow deliberately avoids, so the comment must describe only what the
workflow *does* — do not write the name of the rejected branch-based approach, or any
credential name, anywhere in the file, not even to explain its absence. That rationale
belongs in the SUMMARY, not in the YAML.
  </action>
  <verify>
    <automated>W=.github/workflows/deploy-pages.yml; [ -f "$W" ] &amp;&amp; for s in actions/checkout actions/setup-node actions/configure-pages actions/upload-pages-artifact actions/deploy-pages 'npm ci' 'npm run build' workflow_dispatch 'contents: read' 'pages: write' 'id-token: write' 'concurrency' 'name: github-pages' 'page_url' 'id: deployment' './dist'; do grep -q "$s" "$W" || { echo "MISSING: $s"; exit 1; }; done; [ "$(grep -c 'gh-pages' "$W")" = "0" ] &amp;&amp; [ "$(grep -c 'secrets\.' "$W")" = "0" ] &amp;&amp; [ "$(grep -rn 'import\.meta\.env\.VITE_' src/ vite.config.ts | wc -l | tr -d ' ')" = "0" ] &amp;&amp; node -e "const l=require('fs').readFileSync('.github/workflows/deploy-pages.yml','utf8').split('\n');if(l.some(x=>/\t/.test(x)))throw new Error('tab indentation');if(!l.some(x=>/^on:|^\s*push:/.test(x)))throw new Error('no trigger');console.log('workflow shape OK')"</automated>
  </verify>
  <done>The workflow exists with all five first-party Pages actions, both triggers, the exact three-permission set, a concurrency group, and the `github-pages` environment wired to `page_url`; it references no `gh-pages` branch, no `secrets.*`, and contains no tab indentation; no `import.meta.env.VITE_*` read exists anywhere in `src/` or `vite.config.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> GitHub Pages origin | Everything the build writes into `dist/` becomes world-readable at a public URL. Any build-time-inlined value is published. |
| Browser -> static origin | No server exists. There is no request-time authorization, rewrite, or header control — only files. |
| Repo -> third-party Actions | Any action referenced in the workflow executes with the job's token in the build environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-01 | Information Disclosure | Vite build-time env inlining -> public `dist/assets/*.js` | high | mitigate | No `VITE_`-prefixed variable is introduced. Task 3 gates `grep -rn 'import\.meta\.env\.VITE_' src/ vite.config.ts` to zero, and the workflow declares no `env:` block and no `secrets.*` reference (grep-gated). The Anthropic key stays in the Node-side `chatProxyPlugin`, which is `configureServer`-only and contributes nothing to `vite build`. |
| T-QUICK-02 | Tampering | Supply chain: GitHub Actions + npm install | medium | mitigate | Only first-party `actions/*` actions, pinned to major tags; no third-party publisher action (`gh-pages` explicitly gated out). Build uses `npm ci` against the committed `package-lock.json`, never `npm install`. This plan adds zero npm dependencies, so no new package legitimacy surface exists. |
| T-QUICK-03 | Elevation of Privilege | Workflow `GITHUB_TOKEN` scope | medium | mitigate | Top-level `permissions` limited to exactly `contents: read`, `pages: write`, `id-token: write`. No write access to repository contents, so a compromised build step cannot push code. Grep-gated in Task 3. |
| T-QUICK-04 | Spoofing | Deploy identity to the Pages endpoint | low | accept | `id-token: write` + `actions/deploy-pages` is GitHub's supported OIDC attestation path; the `github-pages` environment provides the trust anchor. Nothing further is available to add on a static host. |
| T-QUICK-05 | Information Disclosure | Pendo agent API key in `index.html` | low | accept | Client-side Pendo API keys are public by design and are already in the committed `index.html`. Explicitly out of scope for this task; publishing does not change the exposure class. |
| T-QUICK-06 | Denial of Service | `ChatLauncher` -> `/api/chat/stream` (404 in production) | low | accept | User-explicitly-accepted known limitation. There is no backend to exhaust; the failure surfaces as a client-side error in the chat panel. Behavior must not be changed (see `<scope_notes>`). |
| T-QUICK-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan, so the Package Legitimacy Gate does not apply. Any future task adding a dependency must run the gate before installing. |
</threat_model>

<verification>
Automated, after all three tasks:

```
npm run typecheck
rm -rf dist && GITHUB_ACTIONS=true npm run build
cmp -s dist/index.html dist/404.html && echo "404 fallback byte-identical"
grep -c '/novus-uat-local/' dist/index.html
grep -rn 'import\.meta\.env\.VITE_' src/ vite.config.ts   # expect no output
grep -rnE 'href=\{?["'"'"'`]/' src/                        # expect no output
rm -rf dist && npm run build && grep -q 'src="/assets/' dist/index.html
```

Dev regression (must be unchanged by this plan):

```
npm run dev    # serves on :3030; visit /, /signup, /app; logos render; chat panel still opens
```

Subpath smoke (approximation only — `vite preview` has its own SPA fallback, so it proves the
base rewrite, not the 404 mechanism):

```
GITHUB_ACTIONS=true npm run build && npm run preview
# open http://localhost:4173/novus-uat-local/  -> landing renders, logo loads,
# "Sign in" link goes to /novus-uat-local/signin
```

<human-check>
After merge to `main`:
1. Set Settings -> Pages -> Source to "GitHub Actions" on `colingm/novus-uat-local` (executor cannot do this).
2. Confirm the Actions run goes green and the `github-pages` environment shows the deployment URL.
3. Load `https://colingm.github.io/novus-uat-local/` — landing page renders with the Halo logo.
4. Sign up, land on `/novus-uat-local/app`, then hard-refresh on `/novus-uat-local/app/reports`
   — the Reports page must render (browser may log a 404 status; that is the accepted caveat).
5. Confirm the chat launcher is still visible and still fails when used — that is the expected,
   accepted state, not a regression.
</human-check>
</verification>

<success_criteria>
- A `GITHUB_ACTIONS=true` build emits `dist/index.html` with `/novus-uat-local/`-prefixed asset
  and favicon URLs, plus a byte-identical `dist/404.html`.
- A plain `npm run build` emits origin-root URLs and the same 404 fallback — local behavior and
  `npm run dev` on port 3030 are unchanged.
- Zero origin-root asset or `href` literals remain in `src/`; the router mounts under
  `import.meta.env.BASE_URL` via `createBrowserRouter`.
- `.github/workflows/deploy-pages.yml` deploys through the first-party Actions Pages path with
  exactly three permissions, a concurrency group, and no repository secrets.
- `ChatLauncher` and `src/chat/claudeClient.ts` are untouched; no `VITE_`-prefixed variable
  exists anywhere in the build.
</success_criteria>

<output>
Create `.planning/quick/260825-cn1-deploy-halo-to-github-pages-as-a-static-/260825-cn1-SUMMARY.md` when done.

Record in the SUMMARY:
- The `<Link>` conversion deferral for the six `href` sites (BASE_URL prefix chosen instead).
- The accepted HTTP-404-status caveat on Pages deep links.
- That agent chat is knowingly broken on the deployed site, and that a serverless proxy is the
  future path if the user wants it fixed.
- The manual "Pages source = GitHub Actions" repo setting as an operator follow-up.
</output>
