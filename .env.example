# Copy to .env and fill in your own key. .env is gitignored — never commit a real key.
# Used by the Agent Chat feature (src/chat) to call the Claude API directly from the browser.
VITE_ANTHROPIC_API_KEY=
